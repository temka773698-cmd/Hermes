# Принудительно используем красивый кириллический домен и HTTPS
RewriteEngine On
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^аквастрой-казань\.рф$ [NC]
RewriteRule ^ https://аквастрой-казань.рф%{REQUEST_URI} [R=301,L]
